// Prisma schema for Guias Bounded Context
generator guiasClient {
    provider   = "prisma-client-js"
    outputMode = "outputDir"
    output     = "../../node_modules/.prisma/guias-client"
}

datasource guiasDb {
    provider = "postgresql"
}

// ===== GUIAS MODELS =====
// Add your guides models here
